/**
 * The GitHub repository URL. Enables the "flag this question" link, which
 * opens a prefilled issue (the PRD's serverless flag endpoint can replace
 * this later). Set to null to hide the link.
 */
export const repositoryUrl: string | null =
  'https://github.com/xariov/queensland-water-rules';
